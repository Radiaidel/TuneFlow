import { TrackSelectors } from '../selectors/track.selectors.selectors';

describe('TrackSelectors', () => {
  it('should create an instance', () => {
    expect(new TrackSelectors()).toBeTruthy();
  });
});
